# 项目公开导出清单

## src/utils 可复用函数/类型

### 1. 顶层文件（直接通过 barrel 导出）

#### sqids.ts ⭐ **在 index.ts 中公开导出**
- `获取注册Sqids()`: 获取用于生成玩家ID的Sqids实例
- `获取联军Sqids()`: 获取用于生成联军ID的Sqids实例

#### TRandom.ts ⭐ **在 index.ts 中公开导出**
- `TRandom(min, mode, max, round?)`: 生成服从三角分布的随机数

#### 插件加载.ts ⭐ **在 index.ts 中公开导出**
- `批量加载插件(ctx, 插件列表, 模块名)`: 批量加载Koishi插件，支持批量日志输出和错误处理

#### 服务记录.ts （**未在 index.ts 中公开导出**）
- `确保服务记录(ctx, 默认字段?)`: 确保服务表中存在必要的记录，用于存储全局运行时状态

---

### 2. 游戏资源 ⭐ **在 index.ts 中公开导出**

#### 图片目录查找.ts
- `查找项目根目录(起始目录)`: 从指定目录向上查找项目根目录（找 package.json）

#### 图片随机生成.ts
- `生成随机图片片段(图片池, 触发概率, 资源子目录?)`: 生成随机图片 HTML 片段，支持概率触发

---

### 3. 解析用户 ⭐ **在 index.ts 中公开导出**

#### types.ts
**类型导出：**
- `联军权限等级` - 联军权限等级类型（1-4）
- `支持平台` - 支持的消息平台类型（onebot | discord | telegram）
- `玩家解析结果` - 玩家解析后的数据结构
- `玩家联军检查选项` - 玩家联军检查时的可选参数
- `玩家联军解析结果` - 玩家联军完整信息结构

#### error.ts （**未在 index.ts 中公开导出**）
- `发送并抛出错误(session, 用户提示, 错误信息?)`: 发送用户消息并抛出错误

#### 会话相关/会话检查.ts
- `会话检查(session)`: 类型断言函数，验证 session 存在，不存在则抛出错误

#### 会话相关/平台检查.ts
- `用户检查(session)`: 检查会话的平台和用户ID，返回规范化的用户信息

#### 玩家相关/获取名称.ts
- `获取玩家展示名称(玩家配置)`: 根据名称是否通过审核返回显示名称

#### 玩家相关/获取数据.ts
**类型导出：**
- `玩家完整资料` - 合并的玩家档案和战争数据

**函数导出：**
- `获取玩家完整资料(ctx, id)`: 从数据库获取玩家的完整资料（档案 + 战争数据）
- `更新玩家资料(ctx, id, 更新数据)`: 更新玩家的档案或战争数据
- `玩家检查(ctx, session)`: 从会话获取并验证玩家信息，返回玩家基本信息和资料

#### 联军相关/权限获取.ts
- `获取联军权限等级(联军资料, uid)`: 计算玩家在联军中的权限等级（1-4）

#### 联军相关/玩家检查.ts
- `玩家联军检查(ctx, session, options?)`: 验证玩家是否在联军中，并检查权限等级

#### 联军相关/贡献统计.ts
**类型导出：**
- `联军贡献条目` - 联军成员贡献排名条目结构

**函数导出：**
- `获取联军贡献排行数据(联军资料)`: 获取联军内所有成员的贡献排行
- `获取成员联军贡献(联军资料, uid)`: 获取特定成员的贡献值
- `获取排除成员后前N贡献总和(联军资料, 排除UID, 数量)`: 计算排除某成员后前N名的贡献总和
- `获取排除成员后平均贡献(联军资料, 排除UID)`: 计算排除某成员后其他成员的平均贡献

---

### 4. 解析目标 ⭐ **在 index.ts 中公开导出**

#### 目标数据解析.ts
- `目标解析(ctx, session, 目标)`: 解析目标玩家信息，支持多种平台的玩家标识

#### 地区相关/机场检查.ts
**类型导出：**
- `机场使用权限检查结果` - 机场权限检查结果结构
- `机场使用权限检查选项` - 机场权限检查选项

**函数导出：**
- `机场使用权限检查(ctx, session, 地区编号?, options?)`: 检查玩家是否可以使用指定地区的机场

#### 地区相关/权限检查.ts
**类型导出：**
- `地区权限检查动作` - 地区权限检查动作类型
- `地区查询权限动作` - 地区查询权限动作类型
- `地区权限检查结果` - 地区权限检查结果结构
- `地区查询权限结果` - 地区查询权限结果结构

**函数导出：**
- `地区查询权限检查(ctx, session, 动作, 地区编号?)`: 检查玩家对地区的查询权限（不涉及修改）

#### 地区相关/获取名称.ts
- `获取地区展示名称(地区配置资料)`: 根据名称审核状态返回地区显示名称

#### 地区相关/获取数据.ts
**类型导出：**
- `地区解析结果` - 地区完整信息结构（包含所有相关数据）

**函数导出：**
- `地区解析(ctx, 目标地区编号, session?)`: 解析地区完整信息，支持按地区编号或平台标识解析
- `当前地区解析(ctx, session)`: 获取会话对应的当前地区完整信息

#### 地区相关/驻扎检查.ts
**类型导出：**
- `驻扎检查结果` - 驻扎权限检查结果结构

**函数导出：**
- `驻扎检查(ctx, session, 地区编号?)`: 检查玩家是否可以在指定地区驻扎

#### 联军相关/获取名称.ts
- `获取联军展示名称(联军资料)`: 根据名称审核状态返回联军显示名称

#### 联军相关/获取数据.ts
- `联军解析(ctx, 目标联军编号)`: 解析联军完整信息

#### 联军相关/获取联军.ts
- `目标联军解析(ctx, session, 目标)`: 解析目标玩家所在的联军信息

---

## src/logic 可复用函数/类型

### 1. 顶层文件

#### 分配地区.ts
- `分配坐标逻辑(ctx, id, 联军编号)`: 根据地区洗牌指针为新加入联军的玩家分配地区

---

### 2. 改名审核 ⭐ **在 index.ts 中公开导出**

#### 工单服务.ts
**类型导出：**
- `改名审核工单` - 改名审核工单的数据结构

**常量导出：**
- `改名工单池` - 存储改名工单的 Map 容器

**函数导出：**
- `解析改名时间戳(上次改名日期)`: 解析改名时间戳（支持 YYYY-MM-DD-HH 格式和标准 Date 格式）
- `检查改名冷却(上次改名日期)`: 检查改名冷却时间是否已过期

---

### 3. 联军相关 ⭐ **在 index.ts 中公开导出**

#### 成员权限.ts
- `获取联军成员权限等级(联军资料, uid)`: 获取成员在联军中的权限等级
- `获取指定等级成员UID列表(联军资料, 权限等级)`: 获取具有指定权限等级的所有成员UID列表
- `设置成员权限等级(联军资料, 目标UID, 权限等级)`: 设置成员权限等级，返回更新后的权限列表

#### 政体策略.ts
**常量导出：**
- `政体默认权限配置映射` - 不同政体下的默认权限配置

**函数导出：**
- 多个权限配置策略函数（民主制、威权制、极权制特定的权限处理逻辑）

#### 移除成员.ts
- `移除联军成员(ctx, { 联军编号, 联军资料, 目标UID, 目标用户ID })`: 从联军中移除成员，并更新相关数据

#### 设置权限.ts
**类型导出：**
- `联军权限动作` - 联军权限动作类型

**常量导出：**
- `联军权限动作列表` - 所有支持的权限动作列表
- `默认联军权限配置` - 默认的权限配置

**函数导出：**
- `校验联军权限等级(value)`: 验证权限等级是否有效（1-4）
- `校验联军权限动作(value)`: 验证权限动作是否有效
- `获取联军权限配置(ctx, session, 动作)`: 获取玩家对特定权限动作的权限要求
- `玩家联军权限设置(ctx, session, 动作)`: 设置或查询玩家的权限配置

#### 扩军计划策略.ts
- `是否豁免扩军计划限制(联军资料, uid, 权限等级)`: 根据联军政体和成员权限判断是否豁免扩军计划限制

---

### 4. 土木工程 ⭐ **在 index.ts 中公开导出**

#### 建造工具.ts
**类型导出：**
- `铁路建造成本结果` - 铁路建造成本计算结果

**函数导出：**
- `获取铁路类型列表()`: 获取所有支持的铁路类型列表
- `获取铁路类型配置(输入)`: 根据类型ID或类型名称获取铁路类型配置
- `计算铁路建造成本({ 铁路类型输入, 地形? })`: 计算铁路建造所需的生产力成本
- `计算建造进度百分比(已投入生产力, 需求生产力)`: 计算建造进度百分比

#### 联军审批.ts
**类型导出：**
- `审批推送平台` - 支持的审批推送平台类型
- `联军审批推送参数` - 联军审批推送参数结构
- `联军审批推送结果` - 联军审批推送结果结构

**函数导出：**
- `请求联军审批(ctx, 参数)`: 请求联军审批，向各平台推送审批消息
- `requestAllianceApproval(ctx, 参数)` (别名): 同上，便于外部调用

#### 铁路建造.ts
**类型导出：**
- `铁路建造玩家快照` - 建造时的玩家数据快照
- `执行铁路修建参数` - 执行铁路修建的参数结构
- `铁路修建执行结果` - 铁路修建执行后的结果
- `创建跨联军铁路申请参数` - 跨联军铁路申请参数结构

**常量导出：**
- `世界银行地区编号` - 世界银行地区的编号常量

**函数导出：**
- 多个铁路建造相关的内部逻辑函数

#### 铁路审批.ts
**类型导出：**
- `铁路审批自动过期结果` - 铁路申请过期扫描结果

**函数导出：**
- `获取有效铁路申请(ctx, 申请ID)`: 获取有效的铁路申请记录，如已过期则标记为过期

---

### 5. 违禁检查 ⭐ **在 index.ts 中公开导出**

#### 检查违禁词.ts
- `检查违禁词(text)`: 使用AC自动机检查文本中的违禁词，返回第一个匹配的违禁词

---

### 6. 信号塔 ⭐ **在 index.ts 中公开导出**

#### 后台/index.ts
**类型导出：**
- `后台信号塔发送参数` - 后台日志发送参数
- `后台信号塔发送记录` - 单条发送记录
- `后台信号塔发送失败记录` - 单条失败记录
- `后台信号塔发送结果` - 发送结果总体结构

**函数导出：**
- `发送后台信号塔日志(ctx, 参数)`: 向各平台的后台频道发送日志

#### 新闻/index.ts
**类型导出（通过 types 导出）：**
- `新闻信号塔发送参数` - 新闻发送参数
- `新闻信号塔发送记录` - 新闻发送记录
- `新闻信号塔发送失败记录` - 新闻发送失败记录
- `新闻信号塔发送结果` - 新闻发送结果

**函数导出：**
- `发送新闻信号塔通报(ctx, 参数)`: 向各平台的新闻频道发送通报

#### 联军/types.ts
**类型导出：**
- `信号塔平台` - 支持的信号塔平台类型
- `信号塔发送参数` - 联军信号塔发送参数
- `信号塔发送记录` - 联军信号塔发送记录
- `信号塔发送失败记录` - 联军信号塔发送失败记录
- `信号塔发送结果` - 联军信号塔发送结果

#### 联军/index.ts
**函数导出：**
- `发送联军信号塔通报(ctx, 参数)`: 向联军首都相关地区发送信号塔通报

#### 地区/index.ts
- 具体内容需查看文件

---

## 导出统计

### src/utils 统计
- **直接导出函数**：6 个（sqids, TRandom, 插件加载, 游戏资源等）
- **类型导出**：7 个
- **子目录导出**：45+ 个函数/类型

### src/logic 统计
- **直接导出函数**：1 个（分配地区）
- **类型导出**：20+ 个
- **子目录导出**：40+ 个函数/类型

### 总计
- **公开导出函数**：约 90+ 个
- **公开导出类型**：约 50+ 个

---

## 使用指南

### 标记说明
- ⭐ **在 index.ts 中公开导出** - 表示该模块/文件通过 barrel export 在主入口文件中公开导出，可直接从 `@/utils` 或 `@/logic` 导入
- （未在 index.ts 中公开导出） - 表示需要直接从子模块导入

### 导入示例
```typescript
// 从公开导出导入
import { TRandom, 获取注册Sqids } from '@/utils';
import { 发送联军信号塔通报, 检查违禁词 } from '@/logic';

// 从未公开导出的子模块导入
import { 发送并抛出错误 } from '@/utils/解析用户/error';
```
