---
description: malie-textgame 架构与下沉脚手架规范
alwaysApply: true
enabled: true
updatedAt: 2026-04-12T04:44:00.000Z
provider:
---

你正在维护 Koishi 插件项目 `malie-textgame`（TypeScript）。这是一个以中文语义命名、以 Koishi 插件组织为核心的文字游戏项目。请严格按照**当前代码库真实存在的下沉脚手架**工作，不要把它想象成一个拥有完整通用后端基础设施的大框架。

---

## 一、先理解这个项目的真实架构

### 1. 总入口与插件式组织
- 项目总入口是 `src/index.ts`。
- 顶层通过 `批量加载插件()` 统一挂载四类模块：
  - `models/` → 数据表注册
  - `services/` → 定时任务与后台调度
  - `指令集/` → 普通玩家指令
  - `管理集/` → 管理员指令
- 这意味着本项目的组织方式首先是 **Koishi 插件式模块组织**，而不是经典 MVC / DDD 模板工程。

### 2. 真实分层哲学
本项目当前更接近：

**“Koishi 插件式组织 + `utils` 解析门面 + `logic` 复杂业务规则容器 + 上层按需编排的实用主义分层。”**

请牢记以下事实：
- `types/` 是底层类型定义层。
- `models/` 是数据库表声明层。
- `utils/` 不是“杂项工具箱”，而是**基础设施辅助 + 解析/校验/安全更新门面**。
- `logic/` 不是纯算法库，而是**复杂业务规则、状态迁移、审核流程、联军策略**的下沉业务层。
- `指令集/`、`管理集/`、`services/` 是上层入口层，负责接入 Koishi 的命令、权限、定时器、消息上下文。

### 3. 不要脑补不存在的基础设施
当前项目里，`src/utils` **并不存在**以下那类通用基建：
- 通用请求封装中心
- 全局缓存层
- 事件总线
- Feature Flag / 特征开关系统
- 统一日志 SDK
- 通用错误处理中间件框架
- 日期/货币/字符串格式化工具库集合

因此：
- **不要凭空新增“看起来很标准”的抽象层。**
- 若需求真的需要新增这类基建，必须先确认项目中是否已有实际使用场景，再做最小下沉，不可直接套用外部工程模板。

---

## 二、`utils/` 的真实职责与使用规范（最高优先级）

### 1. `utils/` 的核心定位
`src/utils` 当前主要由四部分组成：

#### A. 顶层基础设施文件
- `TRandom.ts` → 三角分布随机数生成
- `sqids.ts` → 注册 UID / 联军编号生成入口
- `插件加载.ts` → Koishi 插件批量加载门面
- `服务记录.ts` → 服务记录初始化/确保存在

#### B. `解析用户/`
这是当前项目**最重要的下沉入口之一**，负责：
- 会话断言
- 平台与用户 ID 识别
- 当前玩家解析
- 玩家双表合并读取
- 玩家双表安全更新
- 当前玩家所属联军检查
- 联军权限等级计算
- 联军贡献统计

#### C. `解析目标/`
这是另一个核心入口，负责：
- 目标玩家解析（支持 `@用户` / UID / 平台 ID）
- 目标联军解析
- 地区解析
- 当前群绑定地区解析
- 地区查看/驻扎权限检查
- 地区安全更新

#### D. `随机图片/`
负责图片路径查找与 Koishi 图片片段生成。

### 2. 你必须把 `utils/` 理解为“解析门面”
这里最常复用、也最应该优先检查的不是零散纯函数，而是以下**统一入口**：

- 当前玩家解析 → `玩家检查(ctx, session)`
- 当前玩家联军解析 → `玩家联军检查(ctx, session, options?)`
- 目标玩家解析 → `目标解析(ctx, session, 目标)`
- 地区解析 → `地区解析(ctx, 地区编号, session?)`
- 当前地区解析 → `当前地区解析(ctx, session)`
- 地区权限检查 → `地区查询权限检查()` / `地区驻扎权限设置检查()`
- 玩家资料安全更新 → `更新玩家资料(ctx, id, 更新数据)`
- 地区资料安全更新 → `更新地区资料(ctx, 地区编号, 更新数据)`
- 联军展示/权限辅助 → `获取联军权限等级()`、`获取联军展示名称()`
- 服务记录确保存在 → `确保服务记录(ctx, 默认字段?)`

### 3. 严禁在命令文件中重复实现以下逻辑
若你要开发、修复、重构任何指令或服务，必须先检查 `utils/` 是否已有入口。**已有就必须复用。**

严禁在 `指令集/`、`管理集/` 中重复实现：
- 会话存在性检查
- 平台支持判断
- 当前玩家解析
- 目标玩家解析
- 当前地区 / 目标地区解析
- 玩家是否注册判断
- 玩家是否在联军中判断
- 联军成员列表校验
- 联军权限等级判定
- 地区查看权限判定
- 地区驻扎权限判定
- 玩家双表合并读取
- 玩家双表拆分更新
- 地区表安全更新
- 展示名审核兜底逻辑

### 4. 何时扩展 `utils/`
以下内容优先进入 `utils/`，而不是直接写进命令文件：
- 任何“解析某个实体并顺便做校验”的逻辑
- 任何“返回结构化上下文结果”的逻辑
- 任何“按已有表结构做安全更新”的逻辑
- 任何会被多条命令复用的 guard / resolver / update helper

### 5. `utils/` 的命名风格必须延续现状
必须沿用当前中文动宾命名风格：
- `获取*`
- `检查*`
- `解析*`
- `更新*`
- `设置*`
- `生成*`

不要突然引入英文化命名，不要引入与现有目录风格不一致的抽象名词。

### 6. `utils/` 的返回值风格
`utils/` 里的解析函数倾向于返回**结构化业务对象**，而不是返回多个零散值。新增同类能力时应优先延续这种风格。

例如：
- `玩家检查()` 返回 `id / uid / username / 用户资料 / 用户配置`
- `玩家联军检查()` 在此基础上追加 `联军资料 / 联军编号 / 权限等级`
- `地区解析()` 返回地区表、地形表、状态表、配置表、战略表的合并上下文

### 7. `utils/` 中的导出规则
- 顶层 `src/utils/index.ts` 当前只导出核心分组与少数顶层工具。
- 某些工具（如 `服务记录.ts`）目前通过子路径单独导入。
- **新增共享能力后，必须同步检查是否需要写入对应 `index.ts` barrel 导出。**
- **不要混淆 `@/utils` 顶层导出与子路径导出。**

---

## 三、`logic/` 的真实职责与使用规范

### 1. `logic/` 的核心定位
`src/logic` 当前承载的是**复杂业务规则与工作流**，不是单纯工具函数。

当前主要分为四块：
- `改名审核/` → 改名审核工单、审核流程、冷却与名称校验
- `联军相关/` → 联军权限、成员权限、政体策略、移除成员工作流
- `违禁检查/` → 敏感词加载、归一化、匹配树检测
- `分配地区.ts` → 联军建国时的地区分配工作流

### 2. 你必须把 `logic/` 理解为“复杂业务规则容器”
适合放进 `logic/` 的内容：
- 命名规则、改名冷却、违禁词检查
- 审核流程、工单流转
- 联军权限动作表、旧权限兼容映射
- 政体切换策略
- 联军成员权限重排
- 政变后的权限重置
- 联军成员移除后的联军状态收敛
- 地区分配与回收规则

判断标准：
- 具有明确业务语义，而非单纯 I/O
- 会被多处调用
- 逻辑容易出错或需要统一口径
- 同时可能被命令和服务复用

### 3. `logic/` 不是纯函数层，但应尽量保持规则可复用
当前项目中的 `logic/` 同时存在两类内容：

#### A. 纯规则 / 近纯规则
例如：
- `检查改名冷却()`
- `校验名称文本()`
- `获取政体默认权限配置()`
- `获取政体可设置最小权限等级()`
- `按政体动态分配权限()`
- `设置成员权限等级()`

#### B. 带数据库和 `Context` 的工作流函数
例如：
- `创建改名审核工单()`
- `审核通过改名工单()`
- `检查名称是否重复()`
- `分配坐标逻辑()`
- `移除联军成员()`
- `获取联军权限配置()` / `设置联军权限配置()`

因此你不能把 `logic/` 理解成“禁止碰数据库”的纯 domain 层；它在本项目里是**复杂业务工作流层**。

### 4. 但不要继续扩大 `logic -> utils -> logic` 缠绕
项目现状允许 `logic` 复用 `utils` 中的解析或辅助函数，例如：
- `logic/联军相关/*` 会复用 `获取联军权限等级()`、`更新玩家资料()`、`玩家检查()`

你可以延续这种已有模式，但必须遵守：
- 优先保持依赖方向清晰
- 能写成纯规则就不要引入 `Context`
- 不要为了省事把原本应放 `utils` 的解析逻辑硬塞进 `logic`
- 不要制造新的无意义循环依赖

---

## 四、你必须掌握的几个真实下沉脚手架事实

### 1. 玩家数据是“双表合并资料”
玩家数据并不是单表：
- `Player` → 基础、生产、资源、状态字段
- `PlayerWarData` → 战争、地堡、军备字段
- `PlayerConfig` → 跨平台绑定、展示名、改名记录

业务层读取玩家时，应该理解为：
- `玩家检查()` 返回的是 **`Player + PlayerWarData` 合并资料**，同时附带配置表信息。

必须遵守：
- **禁止**把战争字段当成 `Player` 原生字段来用。
- **禁止**在命令文件中手写玩家主表 + 战争表的拼接。
- **禁止**在命令文件中手写双表字段拆分更新。
- 更新玩家资料时，优先走 `更新玩家资料()`。

### 2. 地区数据是“多表上下文”
一个地区不是单表实体，真实读取通常要同时关注：
- `Region`
- `RegionTerra`
- `RegionState`
- `RegionConfig`
- `RegionStrategy`

因此：
- 地区业务不要假设地区只有一张表。
- 涉及地区查询、权限、展示、驻扎时，优先通过 `地区解析()` / `当前地区解析()` 获取上下文。

### 3. 联军权限体系分两层
联军权限不是单一字段，而是两套机制共同构成：

#### A. 成员当前权限位置
来自联军表本身：
- `联军元首`
- `联军总理`
- `联军一级权限成员列表`
- `联军二级权限成员列表`
- `联军三级权限成员列表`

#### B. 操作所需权限配置
来自联军权限表：
- `成员列表`
- `地区列表`
- `贡献排行`
- `邀请加入联军`
- `设置联军权限`
- `移出联军`
- `查看地区军事` 等

因此：
- **“某成员当前是什么等级”** 与 **“某动作需要什么等级”** 是两套不同概念。
- 前者主要走 `获取联军权限等级()` / `获取联军成员权限等级()`。
- 后者主要走 `获取联军操作权限()` / `玩家联军权限设置()`。

### 4. 改名审核是“内存工单池”，不是持久化工作流
当前 `改名审核/工单服务.ts` 的实现特征非常重要：
- 使用内存 `Map` 维护 `改名工单池`
- 工单号使用进程内自增
- 审核消息通过 onebot 推送到固定审核群
- 工单**当前不会持久化到数据库**

因此：
- 不要误以为这是数据库工单系统。
- 若只是修复/新增同类流程，应优先延续当前机制。
- 除非任务明确要求持久化，否则不要擅自把它重构成数据库工作流。

### 5. 违禁词检查不是简单字符串包含
`logic/违禁检查/` 当前使用的是：
- 文本归一化（`NFKC`、小写、去空白/标点/符号）
- 多路径加载敏感词 JSON
- 匹配树 / fail 指针匹配

因此：
- 不要在命令里重复写一个简化版正则或 `includes()` 来替代它。
- 名称、文本审核相关需求优先复用 `检查违禁词()`。

### 6. 服务表当前存在两个语义记录 ID
当前项目里，`马列服务表` 被分成两类记录使用：
- `id: "service"` → 签到重置、全服统计、联军资本统计等全局日期状态
- `id: "GLOBAL"` → 地区洗牌指针 / 地区分配流程相关状态

这虽然不够统一，但它是**当前真实代码事实**。

因此：
- 修改相关逻辑时必须先确认自己操作的是哪一条记录。
- **不要在普通修复中擅自把两套 ID 语义合并。**
- 若确实要统一，必须作为明确的架构重构任务整体处理。

---

## 五、上层目录的正确职责边界

### 1. `指令集/` 的职责
`指令集/` 应优先只承担：
- Koishi 参数接收
- 基础命令分支判断
- 调用 `utils` / `logic` / 必要数据更新入口
- 组装文本响应

**不要在命令文件中新增大段下列内容：**
- 会复用的业务规则
- 重复的权限判定
- 玩家双表读写拼接
- 地区多表聚合读取
- 改名审核链路
- 联军权限映射规则
- 可下沉的跨表状态迁移

### 2. `管理集/` 的职责
`管理集/` 允许存在更直接的数据库操作，因为它本身就是特权入口。

但是请注意：
- **管理员命令的“直接写库方式”不能被普通业务命令照抄。**
- 看到 `管理集/设置资源.ts` 这类文件时，不要把它当作玩家指令模板。

### 3. `services/` 的职责
`services/` 主要负责：
- `ctx.cron(...)` 定时任务
- 系统状态轮询
- 每日/每小时统计

项目现状中存在一个特殊事实：
- 某些可复用业务函数目前也放在 `services/` 中，例如 `记录联军资本增量()`。

对此请遵守：
- 可以兼容已有模式
- 但**新增可复用业务能力时，优先考虑下沉到 `logic/` 或 `utils/`，不要继续扩散“指令层直接依赖 services”**

### 4. `models/` 的职责
`models/` 只负责：
- `ctx.model.extend(...)`
- 数据表字段声明
- Koishi `Tables` 扩展

严格禁止：
- 在 `models/` 中写业务流程
- 在 `models/` 中写权限判断
- 在 `models/` 中写资源结算
- 在 `models/` 中写命令逻辑

### 5. `types/` 的职责
`types/` 是最底层静态类型层。

严格禁止：
- 让 `types/` 依赖 `utils/`
- 让 `types/` 依赖 `logic/`
- 让 `types/` 依赖 `services/`
- 让 `types/` 依赖 `指令集/` / `管理集/`

---

## 六、AI 开发时必须遵守的落地规则

### 1. 每次动手前先做复用判断
在新增/修复任何业务前，按以下顺序检查：
1. `utils/` 里是否已有解析、检查、更新入口
2. `logic/` 里是否已有业务规则、策略、审核或权限能力
3. 若没有，再决定新增到 `utils/` 还是 `logic/`
4. 最后才修改 `指令集/` / `管理集/` / `services/`

### 2. 判断“该放 `utils` 还是 `logic`”的标准
#### 放进 `utils/` 的场景
- 解析会话/玩家/目标/地区/联军
- 通用权限守卫
- 展示名处理
- 安全更新 helper
- 多表聚合读取
- 会被大量命令直接复用的 guard / resolver

#### 放进 `logic/` 的场景
- 业务规则
- 联军政体策略
- 审核流程
- 冷却逻辑
- 违禁词流程
- 成员权限重排
- 复杂状态迁移
- 具有业务语义的跨表工作流

### 3. 新增共享能力时，不要只改一个地方
每次改动必须闭环：
- 类型定义是否需要更新
- 数据表模型是否需要同步
- barrel 导出是否需要同步
- 所有调用点是否需要统一调整
- 数据库读写口径是否一致

### 4. 以最小改动完成修复，但公共逻辑必须回收
- 若问题只在一个命令中出现，但根因属于公共解析/权限/更新逻辑，应优先抽回 `utils` 或 `logic`。
- 不要在多个命令中分别打补丁。
- 不要把“临时 if”堆在命令文件里长期存在。

### 5. 导入路径与导出风格必须保持统一
- 优先沿用现有别名：`@/utils`、`@/logic`、`@/types`
- 若现有能力只通过子路径导出，则沿用真实子路径
- 新增共享能力时同步维护 `index.ts`
- 不要混用大量无必要的深层相对路径

### 6. 命名风格必须保持中文语义
保持现有项目命名约定：
- 函数名中文化
- 字段名中文化
- 目录语义中文化
- 命令文本中文化

不要为了“通用规范”把已存在的中文语义层强行英文化。

---

## 七、你在本项目中应优先复用的真实入口清单

### 玩家/联军
- `会话检查()`
- `用户检查()`
- `玩家检查()`
- `玩家联军检查()`
- `获取玩家完整资料()`
- `更新玩家资料()`
- `获取联军权限等级()`
- `获取联军贡献排行数据()`
- `获取成员联军贡献()`

### 目标/地区
- `目标解析()`
- `目标联军解析()`
- `联军解析()`
- `地区解析()`
- `当前地区解析()`
- `地区查询权限检查()`
- `地区驻扎权限设置检查()`
- `驻扎检查()`
- `更新地区资料()`

### 联军复杂规则
- `获取联军权限配置()`
- `获取联军操作权限()`
- `设置联军权限配置()`
- `设置联军操作权限()`
- `玩家联军权限设置()`
- `获取政体默认权限配置()`
- `获取政体可设置最小权限等级()`
- `按政体动态分配权限()`
- `政变后权限重置()`
- `设置成员权限等级()`
- `移除联军成员()`

### 改名/审核/违禁
- `检查违禁词()`
- `检查改名冷却()`
- `校验名称文本()`
- `检查名称是否重复()`
- `创建改名审核工单()`
- `解析引用工单编号()`
- `审核通过改名工单()`
- `审核驳回改名工单()`

### 其他基础设施
- `批量加载插件()`
- `确保服务记录()`
- `获取注册Sqids()`
- `获取联军Sqids()`
- `TRandom()`
- `生成随机图片片段()`
- `分配坐标逻辑()`

---

## 八、默认执行策略
当你接到需求时，默认按以下顺序工作：
1. 先定位问题属于 `解析/校验/更新` 还是 `业务规则/策略/流程`
2. 先搜 `utils/` 与 `logic/` 是否已有能力
3. 优先复用，缺失则在正确的下沉层补齐
4. 上层命令只做编排与文本输出
5. 修改完成后检查：类型、模型、导出、调用点、数据库口径是否闭环

---

## 九、回答与提交要求
每次完成代码修改后，你的输出应包含：
- 改动模块清单
- 改动原因说明
- 是否触及 `utils/` / `logic/` 下沉层
- 剩余风险或后续建议

如果发现当前代码存在历史遗留的不统一实现：
- 先说明真实现状
- 再给出最小、兼容当前架构的修复方案
- 不要在未获明确授权时顺手做大规模架构重写

---

## 十、最重要的总原则

**这个项目最重要的下沉脚手架不是“通用技术组件”，而是：**
- `utils/` 提供的 **解析、检查、展示、安全更新门面**
- `logic/` 提供的 **业务规则、审核流程、联军策略、状态迁移容器**

所以你在本项目中工作时，必须优先遵守：

**先看 `utils/`，再看 `logic/`，最后才写上层命令。**

**能复用统一入口就绝不重复实现。**

**能下沉的公共规则必须回收到 `utils/` 或 `logic/`。**
