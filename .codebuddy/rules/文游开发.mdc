---
description:
alwaysApply: true
enabled: true
updatedAt: 2026-04-11T11:01:25.267Z
provider:
---

你正在维护 Koishi 插件项目 `malie-textgame`（TypeScript）。这是一个文字游戏。请严格遵守以下约束。

---

## 项目背景
- **框架**：Koishi
- **语言**：TypeScript
- **代码根目录**：`src/`
- **分层结构**：
  - `models/`     → 数据库表定义
  - `types/`      → 类型定义
  - `utils/`      → 解析函数、通用业务逻辑
  - `指令集/`     → 各业务命令（生产、战争、联军等）
  - `管理集/`     → 管理员命令，平时动的不多
  - `services/`   → 定时任务等后台服务

- **命名风格**：以中文语义为主（函数名、字段名、命令文本等均保持中文），延续现有目录风格与导出方式，**不随意调整模块边界**。

---

## 核心约束（必须遵守）

### 1. `utils` 复用规则（最高优先级）
- **开发 / 修复 / 重构任何指令前，必须先检查 `utils` 中是否已有可复用能力。**
- 若已有能力 → **必须直接复用**，严禁在 `指令集` 中重复实现。
- **严禁在命令文件中重复实现以下逻辑**：
  - 用户 / 会话检查
  - 平台识别
  - 当前玩家解析
  - 目标玩家解析
  - 联军关系校验
  - 权限判断
  - 玩家数据更新

- **玩家相关逻辑统一入口**：
  - 读取当前玩家 → `玩家检查()`
  - 读取目标玩家 → `目标解析()`
  - 联军场景校验 → `玩家联军检查()`
  - 更新玩家资料 → `更新玩家资料()`

- **若现有 `utils` 不完全满足需求**：
  - 优先 **扩展 `utils`**，再让业务指令调用。
  - **禁止将临时逻辑直接堆在命令文件中**。

- **`指令集` 职责限定**：
  - 参数接收
  - 业务流程编排
  - 文本响应返回
  - 所有数据访问、校验、解析、公共规则统一沉淀至 `utils`。

---

### 2. 玩家数据双表结构规则
- 玩家数据由三张表构成：
  - `Player`        → 基础、生产、资源字段
  - `PlayerWarData` → 战争、地堡相关字段
- 业务层读取玩家资料时，应理解为 **“合并资料”**。
- **禁止**将战争字段当作 `Player` 直接字段使用。
- **禁止**在命令文件中手写双表读写逻辑，统一通过 `utils` 封装能力操作。

---

## 修改策略
- 发现重复逻辑时，**优先抽取到 `utils`**，杜绝在多条命令中重复修补同一问题。
- 新功能涉及多命令共用规则 → **先在 `utils` 中实现，再接入业务层**。
- **以最小改动完成修复**，但所有公共逻辑必须回收到 `utils`。

---

## 问题处理与回答要求
- **先定位根因**，再集中修复同类问题。
- **每次改动必须闭环**：
  - 类型定义更新
  - 导入路径调整
  - 所有调用点同步修改
  - 数据库模型一致性检查
- 输出内容必须包含：
  - 改动模块清单
  - 改动原因说明
  - 剩余风险或后续建议
