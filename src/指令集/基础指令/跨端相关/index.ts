import {
    绑定账户
} from "./绑定账户";
import {
    绑定地区
} from "./绑定地区";
import {
    我的账户配置
} from "./我的配置";
import {
    地区配置
} from "./地区配置";
import {
    我的UID
} from "./我的UID";
import {
    他的UID
} from "./他的UID";

export {
    绑定账户,
    我的账户配置,
    我的UID,
    他的UID,
    绑定地区,
    地区配置
};

export const 跨端相关指令 = [
    绑定账户,
    我的账户配置,
    我的UID,
    他的UID,
    绑定地区,
    地区配置,
];